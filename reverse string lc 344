class Solution
{
    public void reverseString(char[] s)
    {
        int l=0;
        int r=s.length-1;
        for (int i=l,j=r;i<j;i++,j--)
        {
            char temp=s[i];
            s[i]=s[j];
            s[j]=temp;
        }
    }
}
